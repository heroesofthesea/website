import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://heroesofthesea.pt',
  // TODO: Add integrations here as needed (e.g., @astrojs/sitemap, @astrojs/image)
  // Future pages to add: /sponsors, /robot — just add .astro files under src/pages/
});
